name: Build macOS App

on:
  push:
    branches:
      - main

jobs:
  build-macos:
    runs-on: macos-14  # Use a stable macOS runner

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 9.0.x

      - name: Accept Xcode License
        run: sudo xcodebuild -license accept

      - name: Ensure Xcode CLI Tools are Installed
        run: |
          sudo rm -rf /Library/Developer/CommandLineTools
          sudo xcode-select --install || true
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcode-select --print-path

      - name: Verify Xcode and MacCatalyst SDK
        run: |
          xcodebuild -version
          xcode-select --print-path
          ls /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/

      - name: Install Required .NET Workloads
        run: |
          dotnet workload install maui maui-maccatalyst
          dotnet workload restore

      - name: Fix Missing Actool and Assets Issue
        run: |
          sudo softwareupdate --all --install --force
          xcrun -find actool || echo "❌ actool missing!"
          xcrun --sdk macosx --show-sdk-path

      - name: Build macOS App
        run: dotnet build -c Release -f net9.0-maccatalyst

      - name: Publish macOS App
        run: dotnet publish -c Release -f net9.0-maccatalyst --no-self-contained

      - name: Upload macOS Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macOS-app
          path: Bloom/bin/Release/net9.0-maccatalyst/maccatalyst-x64/
